server {
    listen 80;
<<<<<<< HEAD
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Crucial para SPAs (React/Vue/Angular): 
        # Si la ruta no existe físicamente, redirige al index.html
        try_files $uri $uri/ /index.html;
    }

    # Opcional: Manejo de errores 500
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
=======
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta cae en index.html (React Router resuelve el resto)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache largo para assets con hash en el nombre
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
>>>>>>> develop
    }
}
